{
    "$schema": "https://railway.app/railway.schema.json",
    "build": {
        "builder": "NIXPACKS",
        "buildCommand": "pnpm --filter backend install && pnpm --filter backend exec prisma generate && pnpm --filter backend run build"
    },
    "deploy": {
        "startCommand": "cd backend && pnpm run start:prod"
    }
}